// components/Tokenomics/TeamReserve/index.tsx
import React from "react";

const TeamReserve = () => (
  <section className="py-12 px-6">
    <h3 className="text-2xl font-semibold mb-2">
      🔹 11% – Team Address / Completion Reserve
    </h3>
    <p>
      Not for profit. For protection. This is the field's buffer – to fill the
      gaps, to finish what must be done, to ensure no garden is left half-grown.
      <br />
      Every use will be transparent, purpose-led, and accountable.
    </p>
  </section>
);

export default TeamReserve;
